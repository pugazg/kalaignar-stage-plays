# Next Chat Prompt — நச்சுக்கோப்பை word-by-word visual fidelity / scans 6–10

Continue directly in `pugazg/kalaignar-stage-plays`, branch `main`, active work `works/nachuk-koppai/`.

## LIVE MAIN IS AUTHORITATIVE
Fetch live main first.

## Controlling source
Use only attached `ACL-TDD_00314_நச்சுக்_கோப்பை.pdf`.

## User-directed fidelity workflow

The active phase is now **visual text fidelity checking of every word/token, 5 physical scans per iteration**.

For each page:
- compare every visible publication-text word/token in reading order;
- include headings, speaker labels, stage directions, punctuation, numerals and Latin-script text;
- verify that non-authorial library/accession marks remain separated;
- mechanical line-wrap joining is allowed only when word identity is unchanged;
- do not normalize from context, grammar, modern spelling, OCR, TDL, Wikisource or another edition;
- make a canonical change only when the attached scan visually proves it;
- unresolved words remain `needs-review`.

## Durable state

- canonical page layer: **63 / 63 COMPLETE**;
- H-GATE coverage: **63 / 63 COMPLETE**;
- word-by-word fidelity coverage: **5 / 63**;
- fidelity Batch 01 scans 1–5: **CLOSED — 5 PASS / 0 needs-review**;
- scan 3 resolved: `திராவிடன் பதிப்பகத்தார்`;
- scan 4 resolved: `ஆதிகேசவலு முதலியார்`, `சிஷ்யன்`;
- fidelity corrections so far: **3**;
- scene assembly: **blocked until full fidelity pass + reconciliation**.

## Exact next activity

Process **Visual text fidelity Batch 02 — scans 6–10**.

Check every word/token on all five source pages. Update the five page records with `visual_text_fidelity`, write a durable Batch 02 fidelity report, update the master fidelity audit and controls, and commit immediately.

Scans 1–5 are closed for the fidelity phase. Do not reopen them unless genuinely stronger source evidence appears.
