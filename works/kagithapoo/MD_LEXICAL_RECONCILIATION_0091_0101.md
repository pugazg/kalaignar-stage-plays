# காகிதப்பூ — MD lexical reconciliation audit, scans 91–101

Status: **RECONCILIATION COMPLETE — APPLICATION PENDING**

Controlling structural source: `TVA_PRL_0001638_முரசொலி_ பொங்கல் மலர்_ 1967.pdf`

Lexical witness: `kaagidha_poo.md` supplied by the user on 2026-09-05.

## Locked reconciliation rule

For this work from this checkpoint onward:

1. **Words / lexical forms come only from `kaagidha_poo.md`.**
2. **PDF controls physical page boundaries, scene/paragraph/speaker structure, reading order, punctuation, brackets, headings, photographs, captions/placement, and other page-layout evidence.**
3. If MD words are misplaced, the same MD words may be repositioned according to PDF structure.
4. If the PDF shows a paragraph/stage-direction/caption for which the MD provides no lexical words, do **not** silently import those words from the PDF. Record a lexical-witness gap.
5. Existing scans 91–101 were originally verified directly against the PDF before this lexical-witness rule was supplied. Their lexical layer therefore requires reconciliation before they can be treated as final under the new rule.

## Scan-by-scan reconciliation

### Scan 91

MD lexical forms differ from the current page record.

Required lexical changes include:

- title `காகிதப்பூ` → MD `காகிதப் பூ`;
- upper-left `காங்கிரசில்` → MD `காங்கிரஸ்`;
- `சோஷலிசம்` → MD `சோசலிசம்`;
- the upper-right callout currently has source-read `பாரிஜாதப்பூ`; MD instead supplies the lexical sequence `காகிதப்பு உரிஜப்பூ` in that location.

The PDF still controls the three flower-callout positions and large-title structure. The unusual MD sequence must not be silently replaced by the PDF word.

### Scan 92

Substantial lexical reconciliation is required while retaining the PDF's Scene-1 layout and punctuation.

Required MD lexical forms include:

- `மற்றுடலினில்` → `மற்றுடலினால்`;
- `தமிழை இழந்தவனே` → `தமிழை இகழ்ந்தவனை`;
- `மாணப்படை` → `மானப்படை`;
- `தமிழ்க்காக்க` → `தமிழ்காக்க`;
- `தியாகிகள் எண்ணிக்கொள்வோம்` → `தியாகிகளை எண்ணிக்கொள்வோம்`;
- `மெளனமாக` → `மௌனமாக`.

The MD places `ராசேந்திரன்` before `மாணவமணி`; the PDF page structure visibly places `மாணவமணி ராசேந்திரன்`. Keep the PDF ordering while using those same MD words.

### Scan 93

Required lexical changes include:

- the performance-note witness is MD `முரசொலி அண்ணாமலரில்`; do not retain a PDF-derived lexical substitute for that word;
- Scene-6 stage direction MD gives `மரகதமும்-மாலாவும் ...`; the current record contains the PDF-derived additional words `அவள் தோழி`.
- `உனக்குப்` → MD `உனக்கு`;
- the first Scene-6 exchange must use the MD lexical sequence including `அந்சு`, `அண்ணா`, `சொம்பு`, `வீரமாதம்` rather than source-read lexical substitutions;
- later dialogue must use MD `சிறப்படையச்செய்த`, `பாராட்டுகிறார்கள்`, `தனி மதிப்படி`, etc.

**Lexical-witness gap:** the source structure contains the words `அவள் தோழி` inside the stage direction, but those words are not supplied at this location by the MD. They must not be imported into the reconciled lexical layer.

### Scan 94

This page contains the largest old-glyph/source-reading divergence in the first batch. The MD lexical witness supersedes the earlier visual lexical readings while the PDF continues to control the narrow-column structure.

Key replacements include the MD sequence:

`சின்னஞ் சிறு பாலகனுக்குச் சீவி முடித்துச் செருமுனைக்கு அனுப்பி வைத்த தாய் களைப்பற்றிய வீரவரிகளைப் புறநானூற்றிலே படித்திருக்கிறோம். ஆனால் நான் இப்போது இருப்பதாகச் சொல்லுகிற அந்தத் தியாக மகளிர்க்குப் புறநானூறுப் பாட்டு இல்லை. அந்த வீரத்தாய்மார்கள் இருக்கிறார்கள் மாலா; இருக்கிறார்கள்.`

This replaces earlier source-read lexical forms including `சேலி`, `தாய்க்குலப் பற்றிய`, `வீர வரலாறு`, `மறந்து போய்த்திருக்கிறோம்`, `புறநானூற்றுப்`, `இல்லே`, and `வீரத் தாய்கள்`.

Further MD lexical corrections include:

- `ஆருயிர்க் கணவன்` → `ஆருயிர்க் கணவனை`;
- `இந்த எதிர்ப்புப் போரில்` → `இந்தி எதிர்ப்புப் போரில்`;
- `அறப் போரில்` → MD lexical `அறப்போரில்`;
- other dialogue words must follow MD lines for Scene 7 while retaining PDF punctuation/paragraph structure.

### Scan 95

Many straightforward MD lexical replacements are available, including:

- `கவலைப்பட்ட ஆரம்பிச்சுட்டீங்களா` → `கவலைப்பட ஆரம்பிச்சுட்டீங்களா`;
- `இல்ல` → `இல்லை`;
- `தந்திருக்கிறார்கள்` → `தந்திருக்கிறீர்கள்`;
- `சூடியிருந்த` → `குடியிருந்த`;
- `பதித்திருக்கிறார்கள்` → `பதித்திருக்கிறீர்கள்`;
- `தாங்கள் இந்தக் கோலத்திற்கு` → `தங்களை இந்தக் கோலத்திற்கு`.

However this scan has **blocking lexical-witness omissions**:

1. The PDF/current page contains a complete `காவேரி:` paragraph beginning with the source-read material `மரகதம்! ... அனுதாபக் ...`; that paragraph is absent from the MD.
2. The following `மரகதம்:` paragraph has a source-structured opening before `அழுது கொண்டிருக்கும் ஏழைமக்களின்...`; the MD omits that opening and jumps directly from the preceding paragraph into `அழுது கொண்டிருக்கும்...`.

Therefore scan 95 cannot remain final/fully reconciled without explicitly representing these MD lexical gaps. No missing words may be copied from the PDF.

### Scan 96

Required MD lexical changes include:

- `மாணப்படை` → `மானப்படை`;
- `எங்கள் மாணவர் அணி` → `எங்கள் மாணவர் உலகம்`;
- `உங்கள் பின்னே` → `உங்கள் பின்னால்`;
- `இந்த எதிர்ப்பின் சின்னம்` → `இந்தி எதிர்ப்பின் சின்னம்`;
- `ஆளுங்கட்சியின்` → MD `ஆளுங் கட்சியின்`;
- the current source-read `இப்போதே முடிவைத்...` sequence must become MD `இப்போதே எழுதி வைத்துக் கொள்ளச்...`;
- `சொல்லுவது` → `சொல்வது`.

PDF continues to control the narrow left-column / upper-right continuation and punctuation.

### Scan 97

Required MD lexical changes include:

- `நிபந்தனை இருக்கிறது` → `நிபந்தனையிருக்கிறது`;
- `கண்ணனைக் தெரியுமல்லவா` → `கண்ணனைத் தெரியுமல்லவா`;
- `அடைப்பட்டோம்` → `அடைபட்டோம்`;
- `திரிவான்` → `திரிவார்`;
- `வித்தியாசமில்லாமல்` → `வித்யாசமில்லாமல்`;
- `நிபந்தனையென்றிருக்கேளே` → `நிபந்தனையென்றீர்களே`;
- source-word divisions are to be re-spaced only where required by PDF structure.

**Lexical-witness gap:** the PDF structure contains the parenthetical stage direction `(படத்திடம் போய்)` inside காவேரி's speech. Those words are absent at this location in the MD. Do not import them from the PDF.

### Scan 98

Required MD lexical changes include:

- `பகைவர்களே` → `பகைவர்களை`;
- `மனதை` → `மனத்தை`;
- `தங்கள் மகன்` → `தங்கள் மகனை`;
- `வெறி கொண்டலையும் என் மகன்` → `வெறி கொண்டலையும் என் மகனை`;
- source-read `(தழுவிக் கொள்கிறாள்)` → MD `(நழுவிக் கொள்ளுதல்)` words while preserving the PDF's parenthetical/stage-direction position;
- `அடி என் கண்ணே ... கண்ணீரைத் துடைத்து` → MD lexical `அம் என் கண்ணே ... கண்ணனைத் திருத்து`;
- `தசாவதரம்` → `தசாவதாரம்`;
- `கண்ணு` → `கண்ணா` where supplied by the MD;
- `செளக்கியமா` → `சௌக்கியமா`;
- `இதுபாரு` → `இதபாரு`;
- the long clothing/police passage must use MD words `ஜெயில்ல`, `இந்தப் புதுத் துணியைப்`, `போலீஸ்காரன்`, `தலைவணங்குறானே`, etc., rather than the prior source-read lexical substitutions.

**Lexical-witness gap / non-dialogue printed text:** the PDF/current page records the printed name `ஓ. எம். சுப்பிரமணியன்,`; the MD contains no `சுப்பிரமணியன்` occurrence. Do not silently treat that caption/name as MD-supported lexical text.

### Scan 99

Required MD lexical changes include:

- `அவதாரங்களுக்கணக்குப்` → `அவதாரங்களைக் கணக்குப்`;
- `உன் வீப் பார்க்கத்தான்` → `உன்னைப் பார்க்கத்தான்`;
- `பல ரூட்டா` → `பஸ் ரூட்டா`;
- `அதான் இப்ப நம்பளூர்` → MD words `அதான் ப்பா நம்ப ஊர்`, placed according to PDF structure;
- `எம்பா` → `ஏம்பா`;
- restore the MD lexical/speaker continuation `கண்: சர்க்கார்` before `உத்திரவுகளையே...`; it is missing from the current page record but the PDF structure places the turn there;
- source-read `காலணி ... காலணி...` passage must use the MD lexical witness `காலனி ஹரிஜனங்களுக்காகக்கட்டி...` while retaining PDF speaker/paragraph structure;
- `அதுஇல்லப்பா` → `அதுஇல்லய்யா`;
- `பாதுகைகள்` → `பாதுகாக்கள்`.

### Scan 100

Required MD lexical changes include:

- `கை கால்கள் உதறுகிறார்` → `கை கால்களை உதறுகிறார்`;
- `நினைச்சியா` → `நெனச்சியா`;
- retain PDF order for the line, but use MD lexical form `கண்ணை`: PDF structure gives the ordering corresponding to `நம்ப விரலாலேயே நம்ப கண்ணை குத்திக் கொள்வாங்களா` rather than blindly following MD word order;
- inside the source parenthetical invitation, `பெரியவர்` → MD `பெரியவா`;
- `பார்த்து` → `பாத்து` and `வாரேன்` → `வர்ரேன்`;
- `பரவாயில்ல` → `பரவாயில்லை`;
- `இல்லீங்க` → `இல்லிங்க`;
- `அறிய தியாகி` → `அரிய தியாகி`;
- `தானய்யா` → `தான்ய்யா`;
- `அண்டு கண்ணு` → `அண்டு கண்ணாணு`;
- `வாரப் பொதுத் தேர்தலிலே` → `வர்ர பொதுத் தேர்தலிலே`;
- `எழுபட்டு` → `ஏழெட்டு`;
- `மாதகாலம்` → MD `மதாகாலம்`;
- `தனிவான` → `தணிவான`;
- `உப்புச்சத்தியாக்கிரகம்` → `உப்புச்சத்யாக்ரகம்`;
- `பார்த்திருக்கேன்` → `பாத்திருக்கேன்`.

### Scan 101

Required MD lexical changes include:

- `இது தெரியாது` → `இது தெரியாதா`;
- `கண்ணு` → MD `கண்ணா` in the applicable turns;
- `கேள்விகளக்` → `கேள்விகளைக்`;
- `ஒருத்தர்னு` → `ஒருத்தானு`;
- source speaker label `சத்யநாத்` must use the MD label word `சத்யநாத` while the PDF continues to control its position/punctuation;
- `என்னுண்ணு` → `என்னான்னு`;
- `பாக்கியமாகக்` → `பாக்யமாக`;
- `ஆட்சேபணை` → `ஆட்சேபனை`;
- `திலவரே` → `தலைவரே`;
- `இதன்` → `இதனைச்`;
- `பேச` → `பேசு`;
- `சும்மா இல்ல` → `சும்மா இல்லை`;
- add MD word `அல்ல` in `இது மண்டபம் அல்ல`; the current page omitted it;
- source-structured `சங்கத்து தலைவர்` → MD lexical `சங்கத்து தலைவன்`;
- the three calls after `மூவரும்:` must retain the PDF's three-way speaker structure while consuming the MD lexical sequence `கண்ணா! கண்ணணு! கண்ணா!` rather than flattening the structure;
- `உங்கள் பொய்யெல்லாம் ஓட்டிவிட்டு` → `உங்களையெல்லாம் ஓட்டிவிட்டு`;
- `பாடி வீட்டாகக்` → `பாடி வீடாகக்`.

## Reconciliation outcome

- scans reviewed against MD lexical witness: **11 / 11 — 91–101**;
- pages with lexical changes required: **11 / 11**;
- MD/source placement issues explicitly identified: **yes**;
- confirmed lexical-witness omissions requiring explicit gap treatment: **scans 93, 95, 97, 98**;
- scan 95 contains the most consequential missing prose and must not be silently completed from the PDF;
- no scan 102+ work is authorized until the 91–101 lexical reconciliation is applied and its gap handling is made durable.

## Next action

Apply this audit to `pages/0091.md`–`0101.md` using the locked rule above. For MD omissions, preserve the PDF structural position but insert an explicit lexical-witness-gap note rather than PDF-derived words; downgrade affected pages from `verified` until the lexical gap is resolved or explicitly accepted by the user. Keep `indexes/page-map.md`, README, HANDOVER, and NEXT_CHAT_PROMPT synchronized after application.